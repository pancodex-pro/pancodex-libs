{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build-all": "yarn --cwd ./packages/domain build && yarn --cwd ./packages/bridge build",
    "dev-all": "yarn --cwd ./packages/domain dev && yarn --cwd ./packages/bridge dev"
  }
}
